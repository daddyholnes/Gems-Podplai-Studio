# AI Model API Keys
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here
PERPLEXITY_API_KEY=your_perplexity_api_key_here

# PostgreSQL Database Configuration (Optional)
# If these variables are not set, the app will use JSON file storage instead
DATABASE_URL=postgresql://username:password@hostname:port/database
# Or individual parameters:
PGUSER=username
PGPASSWORD=password
PGHOST=hostname
PGPORT=5432
PGDATABASE=database_name

# Google Cloud credentials for Vertex AI (Optional)
# The path to the service account key JSON file (relative path from project root)
GOOGLE_APPLICATION_CREDENTIALS=service-account-key.json

# Google OAuth 2.0 Authentication (Required for secure login)
# Create these credentials in Google Cloud Console -> APIs & Services -> Credentials
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
# The URI where users will be redirected after authentication (e.g., your domain)
OAUTH_REDIRECT_URI=https://dartopia.uk/
# Comma-separated list of email domains allowed to access the app (leave empty to allow all)
APPROVED_DOMAINS=dartopia.uk,gmail.com
# Comma-separated list of specific email addresses allowed to access the app
APPROVED_EMAILS=your_email@gmail.com
# Comma-separated list of admin email addresses
ADMIN_EMAILS=your_admin_email@gmail.com

# Application Settings
# Uncomment to set a default theme
# DEFAULT_THEME=amazon_q_purple  # Options: default_blue, amazon_q_purple, dark_teal, midnight, ocean

# Development and Debug Options
# Set to "true" to bypass authentication (only for development)
# BYPASS_AUTH=true
# Set to "true" to bypass OAuth state validation (helps with redirect issues)
# BYPASS_STATE_CHECK=true

# Streamlit server settings - not needed if using the default
# ST_SERVER_PORT=5000
# ST_SERVER_ADDRESS=0.0.0.0